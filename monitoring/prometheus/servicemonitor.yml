apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: openmoodle
  labels: { app.kubernetes.io/name: openmoodle }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: openmoodle } }
  endpoints: [{ port: http, interval: 30s }]