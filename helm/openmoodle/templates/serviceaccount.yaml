{{- if .Values.backup.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "openmoodle.fullname" . }}-backup
  labels: {{- include "openmoodle.labels" . | nindent 4 }}
{{- end }}